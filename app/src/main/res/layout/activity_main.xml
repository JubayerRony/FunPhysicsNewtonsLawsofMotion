<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    android:background="@drawable/home_background">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="?android:attr/selectableItemBackground"
        android:textColor="#ffffff"
        android:text="Laws Of Motion"
        android:id="@+id/lawsButton"
        android:onClick="onClickLaws"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="?android:attr/selectableItemBackground"
        android:textColor="#ffffff"
        android:text="Credits"
        android:id="@+id/creditsButton"
        android:layout_marginTop="22dp"
        android:onClick="onClickCredits"
        android:layout_below="@+id/aboutNewtonButton"
        android:layout_alignRight="@+id/aboutNewtonButton"
        android:layout_alignEnd="@+id/aboutNewtonButton" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="?android:attr/selectableItemBackground"
        android:textColor="#ffffff"
        android:text="About Newton"
        android:id="@+id/aboutNewtonButton"
        android:onClick="onClickAboutNewton"
        android:layout_marginTop="21dp"
        android:layout_below="@+id/lawsButton"
        android:layout_alignRight="@+id/lawsButton"
        android:layout_alignEnd="@+id/lawsButton" />
</RelativeLayout>
